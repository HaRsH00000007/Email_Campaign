# Base URL of the email-campaigning backend.
# Must be reachable from the BROWSER, so use a hostname the browser can resolve
# (not a docker-internal service name).
NEXT_PUBLIC_API_BASE=http://localhost:4000/api/v1
